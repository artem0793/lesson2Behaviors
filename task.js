(function ($) {
  'use strict';

  /**
   * Make rows of the table selectable.
   */
  Drupal.behaviors.task = {
    attach: function (context) {
      // console.log('attach', context);
    },
    detach: function (context) {
      // console.log('detach', context);
    }
  }
})(jQuery);
